# 청첩장 리디자인 계획 (시안 기준)

기준 시안: `design-reference/` (원본 PDF 11p를 파싱해 이미지로 저장)
작성일: 2026-09-12

---

## 1. 시안이 요구하는 것 (수정사항 7개 + 구조)

### 1-1. 명시된 수정 요청 (`design-reference/00-revisions.png`)

| # | 요청 | 현재 상태 | 판정 |
|---|---|---|---|
| 1 | 첫 화면(봉투) 삭제, 링크 클릭 시 바로 표지 | `EnvelopeCover.tsx` 존재 | **삭제 필요** |
| 2 | 참석여부 = 새빨간 하트 아이콘으로 하단 상시 고정 | RSVP 모달은 있으나 상시 버튼 없음 | **신규** |
| 2-1 | 클릭 시 성함/신랑·신부측/참석여부/인원 한 번에 | `RsvpModal.tsx` 존재, 단계 확인 필요 | 개편 |
| 2-2 | 선택창 **위**에 감사인사 대신 날짜·시간·장소·요일 | 현재 감사인사 | 교체 |
| 3 | 노래 토글 상시 표시 (카톡 아이콘은 노란색으로 유사하게) | `MusicToggle.tsx` 존재 | 유지 + 스타일 |
| 3-1 | 곡 지정: Bruno Major - *Nothing* | 현재 파일 = 다른 곡("Heartwarmin…") | **음원 없음** |
| 4 | 배경 = 기본 검정 / 강조색 = 기본 핑크 | 현재 크림+블러쉬 팔레트 | **전면 교체** |
| 5 | 폰트 = Hahmlet 레귤러 (대안: 210 스탠다드) | Noto Serif KR + Cormorant | **교체** |
| 6-1 | 웨딩홀 바로 전화 버튼 | 없음 | **전화번호 없음** |
| 6-2 | 카카오 + 네이버 + **티맵** 3개 링크 | 카카오/네이버만 | 티맵 추가 |
| 7 | 식 다음날부터 링크 폐쇄 (11/15 09:00경) | 없음 | 신규 |
| - | 섹션 제목은 핑크 pill 배지 (갤러리는 제목 없음) | 밑줄형 제목 | 교체 |

### 1-2. 페이지 구조 — **단일 페이지 스크롤 + 전체화면 오버레이**

```
[메인 스크롤 — 라우팅 없음]
  표지 (01)                검정 배경, 레이스 프레임 + 커플사진
  인사말 (02)              검정 배경, 시 인용 + 초대 문구
  인물 선택 (03)           핑크 종이 배경, 사진 3장 → 클릭 시 오버레이 열림
  갤러리 (07)              3x3 = 9장 + '더보기'
  오시는 길 (08)           약도 + 전화 + 지도 3종
  달력/D-DAY, 방명록, 참석여부 (09)

[전체화면 오버레이 — 메인 위에 덮임]
  1-1 신랑 (04)            크림 배경, 편지 2 + 어린시절 + 가족사진 콜라주
  1-2 신부 (05)            크림 배경, 편지 2 + 어린시절 + 가족사진 콜라주
  1-3 우리 이야기 (06)     적갈 배경, 커플사진 + 메모지
  갤러리 더보기 / 사진 확대
  참석여부(RSVP) 모달

[하단 고정 — 항상 표시]
  ♥ 참석여부 · 카톡공유 · 음악 토글
```

**라우팅을 쓰지 않는 이유**
- BGM이 끊기지 않음 (요청 3번의 "상시 재생/토글"과 직결)
- 오버레이를 닫으면 원래 스크롤 위치가 그대로 유지됨 — 복귀 로직 불필요
- 공유 링크는 항상 루트 하나이고, 요청 7번(식 후 링크 폐쇄) 때문에 하위 URL이 존재할 이유가 없음

**대신 반드시 처리할 것**
- 오버레이 열 때 `history.pushState`, `popstate`에서 닫기 → 모바일 뒤로가기/스와이프로 오버레이만 닫히게 (미처리 시 청첩장 자체를 이탈)
- 오버레이 열림 동안 배경 스크롤 잠금
- 오버레이 콘텐츠는 지연 로딩하여 초기 로드 부담 최소화

---

## 2. 재료 검증 결과

### ✅ 확보된 재료

**디자인 토큰 (시안에서 직접 추출한 실제 값)**

| 용도 | 값 |
|---|---|
| 기본 검정 (배경) | `#101010` |
| 기본 핑크 (강조/타이틀) | `#F090A8` |
| 연핑크 (pill 배지, 달력 배경) | `#F8C8D0` |
| 핑크 종이 배경 (페이지1) | `#EFAEBE` |
| 크림 (신랑/신부 페이지) | `#FEF8E2` |
| 적갈 (우리 이야기 페이지) | `#360000` |

**폰트**: `Hahmlet` — `next/font/google`에 존재 확인 (1911종 중 포함). 손글씨 파트용으로 `Nanum Pen Script`, `Gaegu`, `Gamja Flower` 등도 사용 가능.

**사진 (시안 슬롯 ↔ 실제 파일 대조 완료, 이미지 유사도 매칭으로 검증)**

| 시안 위치 | 파일 | 일치도 |
|---|---|---|
| 페이지1 - 신랑 가족사진 | `IMG_4156.JPG` | 확실 |
| 페이지1 - 신부 가족사진 | `IMG_4159.JPG` | 확실 |
| 페이지1 - 우리 이야기 썸네일 | `IMG_6679.JPG` | 확실 |
| 페이지1 - 핑크 종이 배경 | `123F5C50-….JPG` | 확실 |
| 1-1 신랑 - 어머니 편지 | `IMG_4170.JPG` | 확실 |
| 1-1 신랑 - 아버지 편지 | `IMG_6667.JPG` | 확실 |
| 1-1 신랑 - 어린시절 | `IMG_4160.JPG` | 확실 |
| 1-2 신부 - 아버지 편지 | `IMG_4165.JPG` | 확실 |
| 1-2 신부 - 어머니 편지 | `IMG_6647.JPG` | 확실 |
| 1-2 신부 - 어린시절 | `IMG_4158.JPG` (흰배경본) | 누끼본은 PDF에만 |
| 1-3 - 배경(적갈) | `IMG_4174.JPG` | 확실 |
| 1-3 - 버블 사진 | `IMG_6680.JPG` | 확실 |
| 1-3 - 장미 신부 솔로 | `2770172C-….JPG` | 확실 |
| **표지 커플사진** | `IMG_6575.JPG` | 확실 (원본 확보) |
| 갤러리 | 한복/야외 컷 23장 보유 → 전부 수록 | 충분 |

**PDF에서만 추출 가능한 에셋** (이미 `design-reference/assets/`에 저장)
- 섹션 제목 pill 배지 원본
- 달력 디자인 원본
- 신부 어린시절 **투명 누끼본** (public에 있는 건 흰배경본)
- 표지 블롭 마스크 형태 (사진은 원본 `IMG_6575` 사용하고 마스크 모양만 참고)

**사진 이름표 (`public/images/`, 2026-09-15 정리)**

규칙: `파트-번호`. 번호는 그 파트 안에서 보이는 순서다. 사진이 아닌 장식 파일은 `파트-역할`로 쓴다.
한 사진이 두 파트에 나오면 처음 나오는 파트 이름을 쓴다.

**사진을 쓰는 방법 (중요)**

사진은 [app/data/images.ts](app/data/images.ts)에서 `import`로 불러온 값만 쓴다. `"/images/gallery-06.jpg"`
같은 문자열 주소는 쓰지 않는다. 정적 import는 주소에 내용 해시가 붙어(`gallery-06.03jg3e.jpg`)
사진이 바뀌면 주소도 바뀌고, 가로·세로도 파일에서 읽어오므로 따로 적을 필요가 없다.

**사진 순서를 바꿀 때는 파일 내용을 옮기지 말고 배열 순서만 바꾼다.** 2차 수정 때 파일 이름을 두고
내용만 맞바꾼 탓에, 먼저 접속했던 기기에서는 캐시된 옛 사진이 그대로 나와 갤러리 순서가 뒤섞이고
같은 사진이 두 번 보였다(모바일은 9/12 배포본, PC는 최신이 보이는 현상). 파일 이름과 사진을 1:1로
고정해 두면 이 문제가 생기지 않는다.

| 파트 | 파일 | 내용 |
|---|---|---|
| 표지 | `cover-01.jpg` | 커플사진 |
| 표지 | `cover-frame.jpg` | 레이스 프레임 (장식) |
| 인물 선택 | `people-01.jpg` | 신랑 가족사진 (신랑 페이지에도 사용) |
| 인물 선택 | `people-02.jpg` | 신부 가족사진 (신부 페이지에도 사용) |
| 인물 선택 | `people-03.jpg` | 우리 이야기 썸네일 |
| 인물 선택 | `people-04.png` | 신랑 어린시절 (2차 수정 새 사진, 인물선택 2판 전용) |
| 인물 선택 | `people-05.png` | 신부 어린시절 (2차 수정 새 사진, 인물선택 2판 전용) |
| 인물 선택 | `people-heart.png` | 손그림 빨간 하트 (2차 수정 시안에서 추출, 투명) |
| 인물 선택 | `people-bow.png` | 분홍 리본 장식 (2차 수정, 투명) |
| 인사말 | `greeting-bg.jpg` | 흰 편지지 배경 (2차 수정) |
| 신랑 | `groom-01.jpg` | 어머니 편지 (2차 수정 스캔본, 라벤더) |
| 신랑 | `groom-02.jpg` | 아버지 편지 (2차 수정 스캔본, 복숭아) |
| 신랑 | `groom-03.png` | 어린시절 (내부 페이지, 이전 버전 유지) |
| 신부 | `bride-01.jpg` | 아버지 편지 (2차 수정 스캔본, 라벤더) |
| 신부 | `bride-02.jpg` | 어머니 편지 (2차 수정 스캔본, 복숭아) |
| 신부 | `bride-03.png` | 어린시절 (내부 페이지, 이전 버전 유지) |
| 우리 이야기 | `story-01.jpg` | 비눗방울 사진 |
| 우리 이야기 | `story-02.jpg` | 장미 든 신부 |
| 우리 이야기 | `story-03.jpg` | 신부가 쓴 메모 (모눈종이) |
| 우리 이야기 | `story-04.jpg` | 신랑이 쓴 메모 (노란 줄종이) |

| 갤러리 | `gallery-01` ~ `gallery-22.jpg` | 01~09 두 사람이 고른 대표 사진(처음 보이는 3x3) / 10~22 나머지 (22 = 야간 한강 컷) |
| 달력 | `date-glass-left.png`, `date-glass-right.png` | 샴페인잔 일러스트 (장식) |

손글씨 폰트: `public/fonts/KyoboHandwriting2025lyb.ttf` — 교보문고 공식 배포본(상업용 무료).
라이선스가 포맷 변경·개작을 금지하므로 woff2 변환이나 서브셋 없이 원본 그대로 쓴다.

쓰지 않는 파일은 배포되지 않도록 `design-reference/assets/unused/`로 옮겼다
(검정·적갈 배경, 표지 블롭 마스크본, 신부 어린시절 흰배경본, 1차 분홍 종이 배경 `people-bg-v1.jpg`,
안 쓰는 GreatVibes 폰트, 2차 수정으로 교체된 편지·아이사진의 이전 버전 `old-*`).
받은 원본은 `design-reference/assets/source-파트-번호-내용.jpg`로 이름을 붙여 두었다.

**시안 이미지 이름표 (`design-reference/`)**

파일 이름은 모두 영어로 쓰고, 파트 이름은 위 사진 이름표와 같은 단어를 쓴다.

| 파일 | 내용 |
|---|---|
| `00-revisions.png` | 수정사항 7개 |
| `01-cover.png` | 표지 |
| `02-greeting.png` | 인사말 |
| `03-people.png` | 인물 선택 |
| `04-groom.png` | 신랑 상세 |
| `05-bride.png` | 신부 상세 |
| `06-story.png` | 우리 이야기 |
| `07-gallery.png` | 갤러리 |
| `08-venue.png` | 오시는 길 |
| `09-date-guestbook-rsvp.png` | 달력 · 방명록 · 참석여부 |
| `10-date-calendar.png` | 달력 스타일 참고 |
| `assets/date-calendar.png` | 달력 디자인 원본 |
| `assets/section-title-pill.png` | 섹션 제목 pill 배지 |
| `assets/cover-blob-mask.png` | 표지 블롭 마스크 |
| `assets/people-bg.png` | 핑크 종이 배경 원본 |
| `v2-revision-0914/01-people-top.png` | 2차 수정: 인물 선택 상단 |
| `v2-revision-0914/02-people-bottom.png` | 2차 수정: 인물 선택 하단 |
| `v2-revision-0914/03-groom.png` | 2차 수정: 신랑 상세 |
| `v2-revision-0914/04-bride.png` | 2차 수정: 신부 상세 |

### ✅ 확정된 사항 (2026-09-12 사용자 확인)

- **예식일**: 2026년 11월 14일 (토) 12:20 — 달력 시안의 `2026.05.02`는 템플릿 오류로 확정
- **오시는 길**: 약도 이미지 대신 **실제 지도 임베드** 사용
- **웨딩홀 전화**: `02-2111-7000` (웨스턴베니비스 신도림 대표번호) — 전화 버튼에 연결
- **갤러리**: 보유 웨딩 사진을 **전부 수록**, 기본 9장 노출 + 나머지는 '더보기'

### ❌ 아직 없는 재료

| 항목 | 상황 | 필요 조치 |
|---|---|---|
| **BGM 음원** | 현재 `bgm.mp3`는 지정곡이 아닌 다른 곡 | 아래 3절 참고 — **방식 선택만 남음** |
| ~~1-3 메모지 문구~~ | 시안의 "미작성" 상태 **그대로 유지**하기로 결정 | 추후 문구 확보 시 교체 |
| ~~계좌번호~~ | 2026-09-15 실제 번호 반영 (신랑·신부·양가 부모님 6건) | 완료 |
| **계좌 섹션 위치** | 시안에 '마음 전하실 곳'이 없음 | 4페이지(달력·방명록·참석여부) 하단에 배치 예정 |

> ⚠️ **오픈 전 확인할 값**: 신랑·신부 연락처 2건 (계좌번호·메모지 문구는 반영 완료)

---

## 3. BGM (Bruno Major – *Nothing*) 정식 사용 방안

원곡 음원을 배경음악으로 자동재생하려면 **저작권(작사·작곡)** 과 **저작인접권(음반제작자·실연자)** 양쪽의 전송 허락이 모두 필요합니다. 음원을 구매해도 웹사이트 게시(전송) 권리는 별개라 개인이 원본 MP3를 서버에 올리는 방식은 정식 경로가 아닙니다. 현실적인 선택지는 셋입니다.

| 방식 | 합법성 | 시안 요구(토글 UI) 충족 | 비고 |
|---|---|---|---|
| **A. YouTube IFrame Player 임베드** (영상 노출) | ○ 공식 허용 | △ 자체 토글 대신 플레이어 노출 | 플레이어를 숨기고 오디오만 쓰는 건 YouTube 약관 위반 소지 |
| **B. Spotify / Apple Music 공식 임베드 위젯** | ◎ 완전 합법 | △ 자동재생 불가, 사용자 클릭 필요 | 비로그인 시 30초 프리뷰(Spotify) |
| **C. 로열티프리 유사 분위기 곡을 배경음악으로 + 원곡은 링크 버튼** | ◎ 완전 합법 | ◎ 기존 토글 UI 그대로 | 배경음은 다른 곡이 됨 |

**추천: C** — 시안의 "끄고 켜는 토글 상시 표시" 요구를 온전히 만족시키면서 법적 리스크가 없습니다. 원곡은 "신랑신부가 고른 노래" 링크 버튼으로 함께 노출하면 의도도 전달됩니다.
**차선: A** — 원곡을 그대로 들려주는 게 최우선이라면 플레이어를 작게라도 노출하는 형태로 임베드.

> 참고: 흔히 쓰이는 "숨긴 YouTube iframe으로 배경재생"은 손쉽지만 약관 위반 소지가 있어 권하지 않습니다.

---

## 4. 구현 계획

### Phase 1 — 기반 교체 (선행 필수)
1. `globals.css` 팔레트를 시안 토큰(검정/핑크)으로 전면 교체
2. `layout.tsx` 폰트를 Hahmlet + 손글씨 폰트로 교체
3. `EnvelopeCover.tsx` 제거 (요청 1번) — 진입 즉시 표지
4. 공용 오버레이 컴포넌트 작성: 전체화면 레이어 + `history.pushState`/`popstate` 연동 + 배경 스크롤 잠금
5. 공용 컴포넌트: 핑크 pill 섹션 제목, 하단 고정 바(하트 RSVP + 카톡공유 + 음악토글)

### Phase 2 — 에셋 정리
6. PDF에서 투명 누끼 에셋 추출 → `public/images/`로 이동
7. `mock.ts`의 picsum 플레이스홀더를 실제 파일 경로로 전면 교체
8. 사진 파일명을 `파트-번호`로 리네이밍 (`IMG_4170` → `groom-01` 등, 위 사진 이름표 참고)

### Phase 3 — 페이지별 구현
9. 표지: 레이스 프레임 + 블롭 마스크 커플사진 + 날짜/장소
10. 인사말: 시 인용(하현, 〈제목〉) + 초대 문구
11. 페이지1: 핑크 종이 콜라주 + 클릭 네비게이션
12. 1-1/1-2: 편지·사진 콜라주 (기울기, 클립, 리본 장식)
13. 1-3: 적갈 배경 + 메모지 (문구 확보 후)
14. 갤러리: 3x3 + 더보기
15. 오시는 길: 지도 임베드 + 전화 버튼(`02-2111-7000`) + 카카오/네이버/티맵
16. 페이지4: 달력·D-DAY + 방명록 + 참석여부

### Phase 4 — 기능 보완
17. RSVP 모달 개편 (한 화면에 성함/측/참석/인원, 상단에 예식 정보)
18. BGM 교체 (음원 확보 후)
19. 링크 폐쇄 로직 (2026-11-15 09:00 이후 안내 페이지 전환)

---

## 5. 남은 선결 질문

**BGM 방식 하나만 남음** — 위 3절의 A / B / C 중 선택 (추천: C). 이것도 Phase 1~3 착수를 막지 않으며, 기존 토글 UI를 그대로 두고 마지막에 음원만 교체하면 됩니다.

나머지(계좌번호·연락처·메모지 문구)는 더미/미작성 상태로 진행하고, 오픈 전 체크리스트로 관리합니다.
